import React from 'react';
import { View, Text } from 'react-native';

// Landing screen was removed per user request. This placeholder keeps the import safe
export default function Landing() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Landing screen removed. App shows login directly.</Text>
    </View>
  );
}
